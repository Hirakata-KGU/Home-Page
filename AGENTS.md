# プロジェクト概要 & AIアシスタント向けコーディング規約

## 1. プロジェクト概要
- **目的:** 大学 学園祭の公式ホームページ制作
- **フレームワーク:** Nuxt 4.5.2 (Nitro 2.13.4, Vite 8.2.2, Vue 3.5.42)
- **レンダリング方式:** **SSG（静的サイト生成）**
  - **SSR（サーバーサイドレンダリング）は使用しない**
  - GitHub Pagesを想定
- **言語:** TypeScript (Strict mode)
- **スタイリング:** Tailwind CSS + スコープ付きCSS

## 2. 開発・設計の基本原則
- **SSG前提のコード設計:**
  - サーバー専用APIや動的サーバーサイド処理は書かず、完全静的出力可能な実装を行うこと。
  - クライアント側（ブラウザ）でしか動かない処理（`window`, `localStorage`, アニメーション系APIなど）を扱う際は、クライアントサイドでのみ安全に実行されるよう配慮すること。
- **学園祭HPとしての配慮:**
  - 来場者・在学生・受験生がスマートフォンから閲覧する比率が圧倒的に高いため、**モバイルファースト設計**を行うこと。
  - タイムテーブル、企画一覧、アクセスマップなど情報が視覚的・直感的に分かりやすいUIを心がけること。

## 3. スタイリング規約（Tailwind CSS & CSS）
- 原則として **Tailwind CSS のユーティリティクラス** を使用してスタイリングを行うこと。
- **例外（カスタムCSS・@apply の使用許可）:**
  - ボタンやカード、装飾枠など **複数箇所で繰り返し再利用するUIパターン**
  - Tailwindクラスが極端に長くなり保守性を損なう複雑なアニメーションやコンポーネント
  - これらは `<style scoped>` や共通CSSファイルにクラス名（BEMやセマンティックな命名）をつけて定義してよい。